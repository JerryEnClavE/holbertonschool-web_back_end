export default class ClassRoom {
  /**
     * Crea una instancia de ClassRoom.
     * @param {number} maxStudentsSize
     */
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
  }
}
